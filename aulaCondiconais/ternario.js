const saldo = 2;
//imprime real se for 1 senão imprime reais, a ? serve como a indicação de condição
// condição ? expr1: expr2

const unidade = (saldo === 1 ? "real": "reais")
console.log(`voce tem ${saldo} ${unidade}`);



